import { FournisseursPipe } from './fournisseurs.pipe';

describe('FournisseursPipe', () => {
  it('create an instance', () => {
    const pipe = new FournisseursPipe();
    expect(pipe).toBeTruthy();
  });
});
